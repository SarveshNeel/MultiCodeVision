Requirements 

OpenCV 4.x
C++17 Compiler (g++)
pkg-config
make


Usage

Debug Build : make debug
Release Build : make

Run with an image or directory:
    ./build/app <path_to_image_or_folder>
Example:
    ./build/app images/MultiQR.png